# Generated by the video pipeline — rebuild with `npm run video:capture` (or `npm run video:all`).
# Only scripts/video/*.mjs and the published cut in public/video/ are committed.
assets/
audio/
out/
build/
*.log
